begin 
for c in (select c.customerId,c.name,l.Enddate  from customers
inner join loan
on l.customerId=c.customerId)
loop
if c.Enddate between sysdate and sysdate+30 then 
dbms_output.put_line('Remainder: loan for customer'||c.name||'is expiring on'||c.Enddate);
end if;
end loop;
end;
/
